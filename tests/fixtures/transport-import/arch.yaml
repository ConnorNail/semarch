version: 1
include: ["src/**/*.ts"]
domains:
  users:
    root: src/users
components:
  service:
    match: ["**/services/**/*.ts"]
  transport:
    match: ["src/transport/**/*.ts"]
rules:
  - deny: service -> transport
